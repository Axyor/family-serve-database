# Example environment configuration for local development
# Copy this file to .env and adjust values as needed. Do NOT commit your .env.

# MongoDB initialization (used by docker compose)
MONGO_INITDB_DATABASE=family-serve-test
MONGO_INITDB_ROOT_USERNAME=test_user
MONGO_INITDB_ROOT_PASSWORD=test_password

# Optional: application connection string used by examples/scripts
# Adjust host if your Docker is not localhost, and keep authSource=admin for root user
MONGODB_URI=mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@localhost:27017/${MONGO_INITDB_DATABASE}?authSource=admin
